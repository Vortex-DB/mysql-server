#
# The MySQL database server configuration file.
#
#[client]
#user = root
#port            = 3306
#socket          = /tmp/mysql.sock

[mysql]
prompt=\u:\d>\_

# Here is entries for some specific programs
# The following values assume you have at least 32M ram

# This was formally known as [safe_mysqld]. Both versions are currently parsed.
[mysqld_safe]
socket          = /tmp/mysql.sock
#nice           = 0

[mysqld]
#
# * Basic Settings
#
user=root
bind-address=*
port            = 3306
default-storage-engine = innodb
#skip-grant-tables
pid-file        = /home/wurikiji/backup/db/mysql.pid
socket          = /tmp/mysql.sock
datadir         = /home/wurikiji/backup/db
log-error	= /home/wurikiji/logs/mysqld_errors.log
disable_log_bin
character_set_server=latin1
collation_server=latin1_bin
##################################################
# Need to Modify
##################################################
#Log group path (iblog0, iblog1)
innodb_log_group_home_dir=/home/wurikiji/backup/log

#innodb page size
innodb_page_size=4KB

#
# * InnoDB
#

#file per table ON
innodb_use_nvme_hint=false
innodb_file_per_table=1

#buffer settings
innodb_buffer_pool_size=10G
innodb_buffer_pool_instances=8
innodb_max_dirty_pages_pct_lwm=0

#etc
log_error_verbosity=3
innodb_page_cleaners=8

#transaction log settings
innodb_log_file_size=2G
innodb_log_files_in_group=3

# 0:every 1 seconds, 1:fsync on commits, 2:writes on commits
innodb_flush_log_at_trx_commit=0
innodb_log_buffer_size=64M
innodb_flush_neighbors=0

#doublewrite and flush method
innodb_doublewrite=OFF
innodb_flush_method=O_DIRECT

#AIO control
innodb_use_native_aio=true
innodb_change_buffering=none
innodb_lru_scan_depth=1024

# etc settings
max_connections = 1024
open_files_limit = 24000

# io settings
#innodb_io_capacity = 10000
#innodb_io_capacity_max = 20000
#innodb_write_io_threads = 32
#innodb_read_io_threads = 32

# monitoring settings
#performance-schema-instrument='wait/synch/mutex/innodb/%=ON'
#performance-schema-instrument='wait/synch/rwlock/innodb/%=ON'
#innodb_status_output = ON
#innodb_status_output_locks = ON

